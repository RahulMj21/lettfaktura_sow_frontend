import Header from "../components/common/header";

const Home = () => {
  return (
    <div
      className="home page"
      style={{ background: "var(--background-image)" }}
    >
      <Header />
    </div>
  );
};

export default Home;
